import { createStore } from 'vuex'

export default createStore({
  state: {
    isLoading: false,
    showDataSupport: true,
    showDataGithub: true,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
